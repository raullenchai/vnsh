## What this changes

<!-- The behaviour, in a sentence. -->

## Why

<!-- The reasoning is the part that is hard to recover later. What was wrong,
     or what could not be done before? If you considered another approach and
     rejected it, that is worth a line. -->

## How it was checked

<!-- Tests added, and anything verified by hand. For anything touching the
     viewer or the homepage, say whether it was opened in a browser: the
     worker's client-side code lives in a template literal, where a mangled
     backslash compiles cleanly and breaks the page. -->

- [ ] `npm test` passes
- [ ] Tests cover the behaviour, not just the fix
