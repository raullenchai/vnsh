name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-worker:
    name: Worker Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: worker/package-lock.json

      - name: Install dependencies
        run: cd worker && npm ci

      - name: Run tests
        run: cd worker && npm test

  test-mcp:
    name: MCP Server Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mcp/package-lock.json

      - name: Install dependencies
        run: cd mcp && npm ci

      - name: Run tests
        run: cd mcp && npm test

      - name: Build
        run: cd mcp && npm run build

  test-cli:
    name: CLI Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: cli/npm/package-lock.json

      - name: Install dependencies
        run: cd cli/npm && npm ci

      - name: Install MCP dependencies
        # The CLI suite imports the MCP server's crypto to assert both derive
        # byte-identical keys, so that package has to be resolvable here.
        run: cd mcp && npm ci

      - name: Build
        run: cd cli/npm && npm run build

      - name: Run tests
        run: cd cli/npm && npm test

      - name: Test CLI help
        run: node cli/npm/dist/cli.js --help

  test-extension:
    name: Extension Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: extension/package-lock.json

      - name: Install dependencies
        run: cd extension && npm ci

      - name: Run tests
        run: cd extension && npm test

      - name: Build
        run: cd extension && npm run build

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Check formatting (worker)
        run: cd worker && npm ci && npx tsc --noEmit

      - name: Check formatting (mcp)
        run: cd mcp && npm ci && npx tsc --noEmit

      - name: Check formatting (cli)
        run: cd cli/npm && npm ci && npx tsc --noEmit

      - name: Check formatting (extension)
        run: cd extension && npm ci && npx tsc --noEmit
