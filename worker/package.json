{
  "name": "opaque-worker",
  "version": "1.0.0",
  "description": "Host-blind data tunnel API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20241230.0",
    "@vitest/coverage-v8": "^2.1.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.0",
    "wrangler": "^3.99.0"
  }
}
